#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, m, a;
    cin >> n >> m >> a;
    int AreaRect = n * m;
    int AreaSqu = a * a;
    int i = 1;
    while (AreaRect >AreaSqu * i)
    {

    }
    return 0;
}