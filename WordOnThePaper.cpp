#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    string dot[8][8];
    vector<string> name;
    for (int i = 0; i < t; i++)
    {
        for (int j = 0; j < 8; j++)
        {
            for (int k = 0; k < 8; k++)
            {
                cin >> dot[j][k];
                // if (dot[j][k] != '.')
                
                
            }
        }
    }
    return 0;
}